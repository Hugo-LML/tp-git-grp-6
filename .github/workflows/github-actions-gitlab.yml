name: GitlabSync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: hugolml/git-repo-sync@v0.1.0
      with:
        target-url: https://gitlab.com/Hugo-LML/tp-git-grp-6
        target-username: Hugo-LML
        target-token: glpat-So8nparz8YSUD4Nj_syo